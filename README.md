## line-bot
Line bot for personal assistant

### Set up project
`pipenv shell`
`pipenv install`

### Run unit test
`python -m pytest`

### Run local bot server
`python -m app.main`
Flask server is running at localhost:5000
